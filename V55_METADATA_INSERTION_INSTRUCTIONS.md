# V55 repository metadata insertion instructions

Status: repository metadata still pending.

Do not submit the manuscript as READY_TO_SUBMIT until both a real public repository URL and a real archive DOI are inserted.

Required final steps:
1. Create the public GitHub repository.
2. Upload or push the V55 repository release contents.
3. Create a `v1.0.0` release.
4. Archive the release through Zenodo or OSF.
5. Replace the temporary Data Availability sentence in the manuscript with the real repository URL and DOI.
6. Replace `REPOSITORY_URL_REQUIRED_BEFORE_UPLOAD` and `ARCHIVE_DOI_REQUIRED_BEFORE_UPLOAD` in `CITATION.cff`.
7. Recompile the manuscript and recreate the journal upload ZIP.
